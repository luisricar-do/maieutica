"""Agente RAG: documentação Portugol (índice in-memory + grafo LangGraph).

Importar explicitamente o que precisar, por exemplo:
``from agents.rag.graph import COMPILED_RAG_GRAPH`` ou ``from agents.rag.query import build_rag_query``.
"""
