"""Agentes LangGraph do MAS Tutor Socrático.

- ``agents.graph`` — grafo router → (CASUAL: tutor | THEORY: RAG → tutor | DEBUG: analista → RAG → estrategista → tutor) (``tutor_graph``)
- ``agents.rag`` — índice + grafo dedicado ``/api/portugol/ask`` (``get_compiled_rag_graph()``)
"""
